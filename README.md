# Fan Control Script

A simple python script based on psutils and liquidctl for setting fan duty based on CPU temp

Add into ~/.profile script to run in backgroung on login with:
```
python3 /path/to/main.py &  # Ampersand is important!
```
